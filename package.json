{
  "name": "cmas-sim",
  "private": true,
  "version": "0.1.0",
  "description": "CMAS-style ELA practice simulator (TestNav-style) — independent home-use tool",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -k -n server,client -c blue,magenta \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm --workspace server run dev",
    "dev:client": "npm --workspace client run dev",
    "build": "npm --workspace client run build && npm --workspace server run build",
    "build:client": "npm --workspace client run build",
    "build:server": "npm --workspace server run build",
    "start": "node server/dist/index.js",
    "typecheck": "npm --workspace client run typecheck && npm --workspace server run typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
